config_setting(
    name = "windows",
    values = {"cpu": "x64_windows_msvc"}
)

cc_library(
    name = "glfw",
    srcs = select({"windows": glob(["lib-vc2015/*"]),
    "//conditions:default": []}),
    visibility = ["//visibility:public"],
    includes = ["include"]
)