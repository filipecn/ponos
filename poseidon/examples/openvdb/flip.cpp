#include "flip.h"

namespace poseidon {

	bool FLIP::init() {
		// Compute max
	}

	void FLIP::computeDensity() {
		uint maxDimension = dimensions.max();
		size_t size = grid.particleCount();
	}

} // poseidon namespace
